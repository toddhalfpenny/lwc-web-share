<aura:component implements="flexipage:availableForRecordHome">
        <lightning:container
            src="{!$Resource.lwcWebShare + '/index.html'}"
            onmessage="{!c.handleMessage}"
/>
</aura:component>
